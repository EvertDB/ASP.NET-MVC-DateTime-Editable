# ASP.NET-MVC-DateTime-Editable
ASP.NET MVC, no auth, DateTime EditorTemplate

This is a default ASP.NET MVC 5 Web Application without authentication. A custom EditorTemplate for a DateTime was added.
